<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.bkjeon.feature.crawler.NaverShoppingBeautyProduct">

    <insert id="insertNaverShoppingBeautyProduct" parameterType="map">
        INSERT INTO nv_beauty_prod_rank (log_ymd, prod_id, prod_nm, cat_id, rank)
        VALUES (#{logYmd}, #{productNo}, #{productName}, #{categoryNo}, #{productRank})
    </insert>

    <delete id="deleteNaverShoppingBeautyProduct" parameterType="map">
        DELETE FROM nv_beauty_prod_rank WHERE log_ymd = #{logYmd} AND cat_id IN (#{categoryIds})
    </delete>

</mapper>
