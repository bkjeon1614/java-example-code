spring:
  profiles:
    active: local

---

spring:
  profiles: local
  datasource:
    url: jdbc:mysql://ec2-13-125-29-10.ap-northeast-2.compute.amazonaws.com:3000/hs_product?useSSL=false&characterEncoding=utf8
    username: hsdev
    password: wmp2018!@#
    driver-class-name: com.mysql.jdbc.Driver
  devtools:
    livereload:
      enabled: true

mybatis:
  mapper-locations: classpath:mapper/**/*.xml

server:
  port: 8080